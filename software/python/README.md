# career-hello

Phase 0 smoke test buat [[Employment_Action_Plan]] — exit criteria *"satu script Python jalan di
env yang sudah dipilih"*.

**Keputusan env: `uv`.** Dipilih 27 Agu 2026, dipatenkan. Jangan gonta-ganti ke `venv`/`poetry`/
`conda` — master doc bilang pilih satu.

```bash
uv run career-hello
```

Kalau perintah itu jalan dari clean clone tanpa aktivasi manual, env-nya beres.
